#include "main.h"

/**
 * main - prints the number of count
 * @argc: counts the number of arguments
 * @argv: an array for the arguments inputted
 *
 * Return: 0(Success)
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
