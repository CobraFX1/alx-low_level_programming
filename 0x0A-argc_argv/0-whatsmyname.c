#include "main.h"

/**
 * main - prints the name of a program followed by a new line
 * @argc: prints the count of the arguments
 * @argv: an array containing the elements
 *
 * Return: 0(Success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
